export enum StatusType {
    RUNNING = "running",
    STOPPED = "stopped",
    SUSPENDED = "suspended"
}

export enum TaskStatusType {
    FINISHED = "finished",
    PENDING = "pending",
    INITIAL = "initial"
}